# Copy to .env and adjust for local dev. Not used in OpenShift — there, config
# comes from the cluster (Secrets/ConfigMaps), not this file.
POSTGRES_DB=hively
POSTGRES_USER=hively
POSTGRES_PASSWORD=hively_dev
POSTGRES_PORT=5432
